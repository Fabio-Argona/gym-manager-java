name: Deploy Backend to AWS EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Instala git se não tiver
            which git || sudo dnf install -y git

            # Clona ou atualiza o repositório
            if [ -d ~/gym-manager/.git ]; then
              cd ~/gym-manager
              git pull origin main
            else
              git clone https://github.com/Fabio-Argona/gym-manager-java.git ~/gym-manager
              cd ~/gym-manager
            fi

            # Cria o .env com os secrets
            cat > .env <<EOF
            JWT_SECRET=${{ secrets.JWT_SECRET }}
            EOF

            # Sobe os containers (build feito no próprio docker compose)
            docker compose down
            docker compose up --build -d
            docker image prune -f
